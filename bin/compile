#!/bin/bash

cat << EOF

This buildpack has been merged into the official heroku/nodejs buildpack
and is no longer necessary.

You may safely remove it from your app with the following command:

$ heroku buildpacks:remove https://github.com/heroku/heroku-nodejs-metrics-buildpack --app APP_NAME

EOF
